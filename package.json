{
  "name": "node-lib-md",
  "version": "1.0.0",
  "description": "extrair links de um arquivo.md - npm rum cli './caminho' (-- --valida) <- sem parênteses para validar os links",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cli": "node ./src/cli.js"
  },
  "author": "Guilherme Augusto",
  "license": "ISC",
  "dependencies": {
    "chalk": "^5.2.0"
  }
}
